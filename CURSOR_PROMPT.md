# Paste this into a new Cursor chat after opening this folder

Follow OPENBOT.md, AGENTS.md, ARCHITECTURE.md, BRAND.md, and BROWSER.md.
Do not invent a third agent runtime.
Do not vendor Hermes Agent or OpenCode source.
Do not use their logos or claim affiliation.

This instance is for me first: a web board at http://127.0.0.1:8787.
Other people will later spin up their own copy. No multi-tenant cloud.

Week 1 only:
1. Keep the existing stdlib server working.
2. Detect `hermes` and `opencode` on PATH.
3. Status questions read brains/INDEX.md only (no tools).
4. Code changes: if opencode exists, run `opencode run` in the chosen folder; else show an official-installer link and a blocker.
5. Every assistant card shows engine + token/usd estimate fields even if estimate is 0.
6. Footer always shows the three-line credit lockup from BRAND.md.
7. End the job by updating brains/INDEX.md Now / Last / Next / Blocker.

Start by reading those files and brains/INDEX.md, then tell me what is already done and what you will change.
